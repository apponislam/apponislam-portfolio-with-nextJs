import { ModeToggle } from "./mode-toggle";

const NavRightMenu = () => {
    return (
        <nav className="flex items-center gap-4">
            <ModeToggle />
        </nav>
    );
};

export default NavRightMenu;
